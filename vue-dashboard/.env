# Production API Configuration
# In production, this can be set during the Docker build or deployment
# When VITE_API_URL is not set, it will automatically use the current domain

# For local development, set this to your local API server
# VITE_API_URL=http://localhost:8080

# For production builds, the CI/CD pipeline can set this
# VITE_API_URL=http://13.59.238.219:8080

# Auto-detect production URL
VITE_API_URL=auto

# Environment
NODE_ENV=production